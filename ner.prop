# Annotators that should be used by CoreNLP. The important ones are ner and
# entitymentions; the preceeding ones are prerequisites for their use.
annotators = tokenize,ssplit,pos,lemma,ner,entitymentions

# Don't look for numbers right now.
ner.applyNumericClassifiers = false

# Don't throw away newlines, because they're useful for figuring out where the titles
# are.
tokenize.keepeol = true
